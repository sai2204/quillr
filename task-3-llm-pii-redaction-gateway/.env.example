# port the redaction gateway's HTTP server listens on
GATEWAY_PORT=5000

# port the mock streaming upstream LLM listens on
UPSTREAM_PORT=5100

# full URL (including path) the gateway streams the upstream request from
UPSTREAM_URL=http://localhost:5100/v1/chat/stream
